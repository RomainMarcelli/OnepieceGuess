version: '3.9'

services:
  app:
    build:
      context: ./src
    ports:
      - "3000:3000"
    volumes:
      - ./src:/app
      - /app/node_modules
    environment:
      NODE_ENV: production
    working_dir: /app
    command: ["node", "index.js"]
